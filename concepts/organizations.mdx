---
title: "Organizations and Multi-Tenant Access in Raptor Comply"
description: "Each Raptor Comply account belongs to an organization tenant. Learn how organizations isolate your data and how an API key is scoped to a single organization."
---

> Each Raptor Comply account belongs to an organization tenant. Learn how organizations isolate your data and how an API key is scoped to a single organization.

Every Raptor Comply account is scoped to an **organization**: a private tenant that holds all of your compliance data, users, and configuration. Each API key is issued inside one organization and is scoped to that organization alone. The key identifies the tenant on its own, so every request resolves against the right organization with no slug to supply and no second header to set.

## Organization scope

All data in Raptor Comply, including facilities, control centers, cyber systems, cyber assets, users, policy documents, tasks, evidence, and training records, belongs to a specific organization. An API key issued by one organization cannot read or modify data in any other organization. The key carries its own organization, so a request can only ever reach the tenant that key belongs to.

This means your integration cannot accidentally leak or merge data across NERC entities. Each organization is a fully isolated data boundary.

## Multiple organizations

If your company operates multiple NERC-registered entities, or manages compliance for more than one registered entity in separate tenants, you may have access to more than one Raptor Comply organization. Each organization requires its own API key, scoped to that tenant.

You cannot use a single API key across multiple tenants. Structure your integration to accept the key as a configurable parameter, so you can run a separate instance or job for each organization you manage.

`GET /users/me/organizations`, which will list all organizations your user account can access, is coming soon. Until then, coordinate with your organization admins to collect an API key for each tenant you need to reach.

## Vendor access

If you are an integration vendor or systems integrator building a connection on behalf of a utility customer, you need an API key scoped to the **customer's** organization, not your own. Your customer's Organization Admin must generate the key inside their tenant and share it with you through a secure channel. Raptor Comply does not email keys, so use your standard secrets-sharing process.

Once you have the customer's key, treat it like any other organization credential: store it securely, never commit it to source code, and rotate it if it is exposed.
